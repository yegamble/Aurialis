services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - smart-split-temp:/tmp/smart-split
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  smart-split-temp:
