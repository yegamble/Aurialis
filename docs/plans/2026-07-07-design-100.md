# Aurialis — Design 100% (Aurialis Redesign.html parity + feature completion)

Status: COMPLETE (Stage 5 local verification passed 2026-07-08)
Branch: `feat/design-100`
Source of truth: Claude Design project `cf2e6298-7dd3-4d1f-a902-4c5d39f4b578`, file
`Aurialis Redesign.html` (Direction A · Aurialis Studio). Local reference copies of the
prototype JSX live outside the repo (session scratchpad `design/` dir).
Derived from: 5-agent design-vs-code diff (2026-07-07) + implementation audit + user directives.

## User directives (2026-07-07)

1. Deep Mastering and Smart Master Album must be **verified working locally** (dev
   server + backend + browser) before anything is treated as done.
2. Keep a version of the **Direction C goniometer** ("Phase Scope" console treatment).
3. **Mobile must look clean** and must not hide/cut off the audio-player visuals and
   animation — bring compact versions to small viewports instead of hiding them.

## Rules

- Every task: RED test → GREEN impl → `pnpm exec tsc --noEmit && pnpm lint && pnpm vitest run`
  green → one atomic commit. Playwright suites run at stage gates (not every commit).
- Preserve every existing `data-testid` contract; update specs in the same commit as any
  DOM change that must move one.
- Design fidelity never trumps truth: no fake labels (no "Mid/Side" caption unless the
  waveform actually renders M/S), no dead buttons, no mocked numbers presented as real.
- Prototype conceits stay out: WindowChrome traffic lights (removed in 3651daa — settled),
  mock BPM/key/artist metadata, sine-wave sim meters.

## Stage 1 — Shell unification (sequential, blocks everything)

| # | Task |
|---|---|
| 1.1 | `AppShell` gains a full-height workspace slot (`variant="workspace"` or `fullBleed` prop): children render directly in the flex row (no imposed single-column `<main>` scroll wrapper), Sidebar rendered unconditionally on desktop. Keep `data-testid="app-shell"` + `role="main"` landmark on all variants. |
| 1.2 | `/master` composes AppShell (drop hand-rolled `flex h-screen` + `isLgViewport` sidebar gate). All existing E2E testids preserved. |
| 1.3 | `/mix` (StemsView) composes AppShell the same way. |

## Stage 2 — Parallel area tracks (worktrees off Stage 1)

### B · Sidebar (src/components/shell/*)
- B1 SearchBar at top: "Search library" placeholder, ⌘K kbd chip; ⌘K focuses it; typing
  filters the sidebar track list (and library list on `/` via optional callback).
- B2 Footer status block alongside Pro Mode toggle: real OPFS usage
  (`navigator.storage.estimate()`) + backend health dot (`checkBackendHealth`), polled lazily.
- B3 NavTrack rows: deterministic gradient ArtTile (seeded by entry id) + trailing
  analyzed-check when the entry has saved analysis. `SidebarTrack` gains optional fields —
  all new props optional so pages compile untouched.
- B4 Tracks NavGroup label = collection title when known, fallback "Tracks".

### C · Master screen (src/app/master/*, src/components/mastering/*, src/components/visualization/*)
- C1 Transport as a bordered card, three-zone: A/B pill left · controls center · time right.
- C2 Skip-forward (+5 s) button.
- C3 Waveform card header: current time left · "Waveform" caption center · duration right
  (honest label — no M/S claim).
- C4 Spectrum card header: "Spectrum" · "20 Hz — 20 kHz · Bus output".
- C5 **Phase Scope (Direction C port)**: pro-mode goniometer panel restyled to the Console
  treatment — "Phase Scope" header, correlation status line ("Mostly correlated · +0.42"
  style, green/red dot from live correlation). Keep `master-goniometer-slot`, `goniometer-canvas`,
  pro-mode.spec.ts green.
- C6 Deep timeline promoted to a full-width main-canvas card below transport (deep mode +
  script ready): header "Mastering Script · {profile}" + Move/Edited/AI-Repair legend;
  inspector keeps profile/analyze controls. Move editor anchored top-right (no x-offset overflow).
- C7 MasterToolbar right-side actions: "Split stems" (→ /mix) + primary "Export".
- C8 Export becomes a focused state/overlay on /master (design's dedicated screen intent):
  header + "Back to mastering"; reached via toolbar Export. `export-view` testid preserved.
- C9 Simple panel surfaces the Output-target platform selector (already wired via props).
- C10 Toolbar meta: generated ArtTile thumbnail (seeded), keep real meta only.

### D · Library + Import (src/app/page.tsx, src/components/library/*, src/components/upload/*)
- D1 Wire AlbumHeroCard on `/`: album summary derived from entries (avg LUFS, computed
  variance + issues count from measuredLufs), CTA → /album.
- D2 Persistent inline dashed dropzone after the library list (reuses upload handler).
- D3 In-shell Import view: compact "Import / Add audio" header + Cancel → library.
  Marketing hero remains only for the true first-run (empty library) state.
- D4 Sidebar "Import" reaches the import screen from every route (`/?screen=upload`).
- D5 Library table: column-header row; columns from real data only — Title (+ArtTile),
  LUFS (amber when off-target), Length, Added/Modified. No BPM/key/genre (no data).

### E · Smart Master Album (src/app/album/*, src/components/album/*, src/lib/album/*)
- E1 LufsBarChart: y-gridlines, labeled target line, per-track bars at measured LUFS,
  signed delta labels, drift coloring (|Δ|>1.5 amber), clickable → open track.
- E2 Hero stats column: Target · Range (min→max) · Issues (count |Δ|>1.5, amber when >0).
- E3 Two cards: "Loudness across album" (chart) + "Smart suggestions" (severity dot +
  3-tier copy, thresholds 1.5/0.8 LU).
- E4 Consistency segmented: LUFS active; Tonal/Dynamics visible but disabled (honest).
- E5 **Master entire album (client-side)**: batch pipeline — for each analyzed entry, load
  OPFS audio, apply persisted chain with targetLufs overridden to album target,
  `renderOffline`, encode, ZIP download. Per-track progress UI; cancellable; errors
  surfaced per track. Pure orchestration logic extracted + unit-tested.
- E6 New `e2e/album.spec.ts`: empty state, rows from seeded entries, chart renders,
  master-all downloads a ZIP (smallest fixture).
- Skipped deliberately: "Check consistency" re-analyze CTA (deltas already live-computed).

### F · Export + Mix (src/components/export/*, src/components/mixer/*, src/components/mix/*)
- F1 Format segmented: WAV / MP3 (wasm or js encoder) / FLAC (best-effort; if no solid
  browser encoder, ship WAV+MP3 and document). AAC omitted — no viable browser encoder.
- F2 Sample-rate segmented incl. 192 kHz; dither as a toggle (on = TPDF).
- F3 Estimated-size row (duration × SR × bit-depth × channels).
- F4 Export progress bar with % (renderer progress callback if available, else staged).
- F5 /mix "Export Mix" routes through the same export options (no more hardcoded 16/44.1).
- F6 Separation progress hero treatment (icon + headline + stage pills), testids preserved.
- F7 "Send these stems to mastering?" CTA card wired to existing onSendToMaster.

### G · R2 test debt (backend/tests/*, e2e/*)
- G1 `backend/tests/test_main_json_endpoints.py` via httpx MockTransport (no respx dep).
- G2 `e2e/r2-upload.spec.ts` covering the direct-to-R2 JSON path (route-mocked Worker).
- HOLD: legacy multipart fallback removal + real upload against deployed Worker — needs
  live infra + explicit user go-ahead (it replaces a working path).

**Live verification (2026-07-08) — THREE blockers found, TWO code-fixed on `feat/design-100`:**
- Blocker 1 — Turnstile verified on BOTH `/upload/initiate` and `/upload/complete` while
  the client sent the same single-use token to each → complete always 403'd. **CODE-FIXED
  (51758f6):** verify on initiate only; complete authorized by the uploadId+key capability.
- Blocker 2 — client bundle had no site key (`NEXT_PUBLIC_TURNSTILE_SITE_KEY` was a Worker
  runtime var, but Next inlines `NEXT_PUBLIC_*` at build time) → all uploads fell back to
  multipart. **CODE-FIXED (9b7a222):** key added to committed `.env.production`; `pnpm build`
  confirms it inlines into a client chunk.
- Blocker 3 — missing R2 secrets threw an uncaught 500 with NO CORS at the SigV4 presign
  step (opaque ERR_FAILED). **Code hardened (d4905ea):** explicit `503 {"detail":"R2 storage
  not configured"}` WITH CORS + top-level CORS-safe catch — but the upload stays BLOCKED
  until the secrets are set (infra).

**Remaining INFRA steps (held; needs go-ahead), in order:**
1. Set `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_ACCOUNT_ID` secrets on `aurialis-core`.
2. Redeploy BOTH workers — `aurialis-core` (worker code fixes) and `aurialis` (rebuild bakes
   the inlined site key into the client bundle).

  **✅ CUTOVER COMPLETE — live-verified 2026-07-08 (all held steps executed):**
  R2 + Analytics Engine + Workers Paid re-enabled on the account (a billing
  lapse had silently disabled all three — the "10136 enable R2" and containers
  "Unauthorized" errors were the account plan, not tokens); bucket + lifecycle
  + fresh secrets provisioned; both workers + the container image deployed
  (CI token gained R2 + Cloudchamber scopes; wrangler bumped to 4.108).
  Live end-to-end verification from the production browser: Turnstile gate →
  initiate 200 → part PUTs → complete 200 → /analyze/deep JSON → real deep
  analysis to done/100% with the script rendered. Two further production-only
  bugs found and fixed during verification: quoted S3 part ETags rejected by
  the R2 binding's complete() (worker now normalizes), and a double-consumed
  httpx stream in backend r2_download (StreamConsumed → bare 500 on every
  JSON download; now single-pass with a one-shot-stream regression test).
  **The legacy multipart fallback is REMOVED** (src/lib/api/upload-strategy.ts):
  site key configured → R2 required with an actionable no-token error; no site
  key → dev-only multipart (local docker). Container error passthroughs now
  carry CORS. [closed 2026-07-08]

3. Re-verify a real upload against the deployed Worker (initiate → PUT parts → complete → analyze).
4. THEN remove the legacy multipart fallback.

## Stage 3 — Merge + gate
Merge worktree branches into `feat/design-100`; full gate: tsc, lint, vitest, full
Playwright suite locally.

## Stage 4 — Mobile polish (user directive 3)
Audit /, /master, /mix, /album at 375×812 and 768×1024: no horizontal cut-off; waveform,
meters and spectrum get compact variants instead of being hidden; animations retained
(respect prefers-reduced-motion). Fix + screenshot evidence.

## Stage 5 — Local verification (user directive 1) — DONE 2026-07-08
Fresh-eyes agent, full stack up (Next server + backend via docker compose): browser
walkthroughs of Deep Mastering (profile → analyze → timeline → A/B → export) and Smart
Master Album (rows, chart, master-all ZIP) + full test suites. Anything broken loops back
to its area. Only then is the plan DONE.

Results (real browser + real backend, screenshots in session scratchpad `verify/`):
- [x] **Deep Mastering WORKS**: upload → Deep → non-default profile (Metal Wall) →
  Analyze runs the real sections→stems→script pipeline; progress card cycles stages;
  on completion the page does NOT crash (the 53f4d45 "script.moves is not iterable"
  regression stays fixed); full-width deep timeline + Move/Edited/AI-Repair legend
  renders; profile-switch Apply regenerates the script; move editor opens top-right
  and an edit flips `edited`; Script A/B toggle works; toolbar Export → WAV + MP3
  downloads (valid RIFF / MPEG-frame headers).
- [x] **Smart Master Album WORKS**: 3 seeded entries (distinct measured LUFS via the
  sanctioned `window.__aurialisLibraryStore` + `updateMeasuredLufs` test seam) →
  hero + stats (Target · Range · Issues) + LUFS bar chart (positioned bars, target
  line, signed deltas, drift colouring) + 3-tier suggestions + LUFS-only segmented;
  Master entire album → per-track progress + ZIP (one WAV/track, decodable).
  **Independently verified** each mastered track integrates to ~−14 LUFS
  (−14.06 / −14.11 / −14.25, pyloudnorm via `backend/.venv`) — album re-gain lands.
  Chart-bar click opens that track on /master.
- [x] **Mobile 375×812 WORKS**: drawer nav opens/closes on /, /master, /mix, /album;
  /master shows compact meters + waveform + Phase Scope (Pro Mode); no horizontal cut-off.
- [x] **Full test gate green** (prod E2E bundle, `NEXT_PUBLIC_E2E_HOOKS=1`, matches CI):
  tsc 0 · lint 0 · vitest 1607 pass (2 CPU-timing micro-benchmarks flake only under
  concurrent docker+server load, pass in isolation) · Playwright 100 pass / 4 skip /
  0 fail (4 skips = smart-split separation TS-001×3 + TS-005×1, pre-existing
  collection-time skip that also skips in CI) · backend pytest 115 pass.
- Test-infra fixes to make the deep-mastering + smart-split + mixer specs actually run
  and pass against the real backend: d8937d9, d705585, aae00a2, cc038ed.

## Acceptance
- [x] All Stage 1–5 boxes ticked, tree green (tsc 0 / lint 0 / vitest 0 fail / Playwright 0 fail locally).
- [x] /, /master, /mix, /album all render the unified shell with design-parity layouts.
- [x] Deep Mastering + Smart Master Album verified working locally in a real browser.
- [x] Direction C Phase Scope live behind Pro Mode; mobile keeps compact player visuals.
- [x] `ralph/fix_plan.md` updated to reflect the new state.
